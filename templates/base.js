const make_button = document.querySelector("#make_button")
const get_entries = document.querySelectorAll(".get_entries")

function updateEntries(data) {
    const options = [];
    for (const name of 
}